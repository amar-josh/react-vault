/**
 * shadcn/ui primitives. Add components as `pnpm dlx shadcn-ui@latest add <component>`
 * when scaffolding a new app — they get copied into this directory and become
 * part of the design system.
 *
 * v0.1: empty. Apps install primitives via shadcn CLI into their own src/components/ui/.
 * Future: pre-install the standard BFSI set (Button, Input, Form, Dialog, Toast,
 * Table, Tabs, Tooltip, Combobox, DatePicker) here so apps inherit them.
 */
export {};
